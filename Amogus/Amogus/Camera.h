#pragma once
class Camera
{
private:
	//int m_viewportWidth;
	//int m_viewportHeight;

public:
	int m_viewportWidth;
	int m_viewportHeight;

	//glm::mat4 view;

	//void SetViewportWidth(int newWidth) { m_viewportWidth = newWidth; }
	//void SetViewportHeight(int newHeight) { m_viewportWidth = newHeight; }
	//void SetView(glm::vec3 position, glm::vec3 target, glm::vec3 vector) { view = glm::lookAt(position, target, vector);	}

	//int GetViewportWidth() { return m_viewportWidth; }
	//int GetViewportHeight() { return m_viewportHeight; }
	//glm::mat4 GetView() { return view; }
};

